# Azure OpenAI Configuration
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_KEY=your-api-key-here
# IMPORTANT: Use "preview" for Responses API (required for reasoning models like  o1, o3)
# Use standard versions like "2024-10-21" for regular chat models
# AZURE_OPENAI_DEPLOYMENT will be used fo root cause analysis mode.
# AZURE_OPENAI_CLASSIFIER_DEPLOYMENT will be used for classifier model.
AZURE_OPENAI_API_VERSION=preview
AZURE_OPENAI_DEPLOYMENT=gpt-4.1
AZURE_OPENAI_CLASSIFIER_DEPLOYMENT=gpt-4.1-mini
AZURE_OPENAI_EMBEDDING_MODEL_NAME=text-embedding-3-small
AZURE_OPENAI_EMBEDDING_MODEL_VERSION=2023-05-15
AZURE_AI_SEARCH_SERVICE_NAME=search-service-name
AZURE_AI_SEARCH_INDEX_NAME=index-name
AZURE_SEARCH_API_KEY=azure-search-api-key
AZURE_AI_SEARCH_API_VERSION=2025-09-01

# Tools API Configuration
KUBE_TOOLS_BASE_URL=http://your-kube-tools-api/api
NON_KUBE_TOOLS_BASE_URL=http://your-non-kube-tools-api/api
TOOLS_API_KEY=your-api-key-here
WEBHOOK_API_KEY=


# Classifier deployment for router, k8s-monitoring, and rag agents
AZURE_OPENAI_CLASSIFIER_DEPLOYMENT=gpt-4.1-mini

# Agent Behavior Configuration
AGENT_TEMPERATURE=0.7
AGENT_TOP_P=0.95
# AGENT_MAX_TOKENS=
# Reasoning effort for reasoning models (low/medium/high)
AGENT_REASONING_EFFORT=medium

# Langfuse Observability (optional)
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=https://cloud.langfuse.com

# App Port
APP_PORT=8001

# OpenTelemetry Configuration
ENABLE_OTEL=true
ENVIRONMENT=production
OTEL_ENABLE_SENSITIVE_DATA=true

# Microsoft Teams notifications (Adaptive Cards)
# Incoming webhook URL from a Power Automate "Workflows" flow
# ("Post to a channel when a webhook request is received") or a classic
# Office 365 connector. Leave empty to disable Teams delivery.
TEAMS_WEBHOOK_URL=
TEAMS_NOTIFICATIONS_ENABLED=true

# Teams outgoing-webhook / bot HMAC secret (base64). When set, the
# /api/teams/messages endpoint verifies the HMAC signature on each request.
TEAMS_OUTGOING_WEBHOOK_SECRET=

# Azure Bot (Bot Framework). When MICROSOFT_APP_ID is set, /api/teams/messages
# requires a valid Bot Framework JWT. App password is used for outbound/proactive
# messages (see docs/TEAMS_BOT.md).
MICROSOFT_APP_ID=
MICROSOFT_APP_PASSWORD=

# Remediation executor backend for the approval demo:
#   (unset) -> kubernetes (kubectl rollout restart, default)
#   ansible -> run a real ansible-playbook (deploy/ansible/restart_app.yml)
#   awx     -> launch an AWX/Tower job template over REST (requires AWX_URL)
EXECUTOR=
AWX_URL=
AWX_TOKEN=

# LLM provider for the analysis/recommendation narrative (detection + remediation
# stay rule-based). Options: azure | deepseek | none. Unset => auto-detect by which
# credentials are present; falls back to a deterministic template if none.
LLM_PROVIDER=

# DeepSeek (OpenAI-compatible)
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# Azure OpenAI chat completions (reuses AZURE_OPENAI_ENDPOINT / AZURE_OPENAI_API_KEY above)
AZURE_OPENAI_CHAT_DEPLOYMENT=gpt-4o
AZURE_OPENAI_CHAT_API_VERSION=2024-10-21
